package edu.arnulfo.ramos.reto10.process;

/**
 La interfaz Interviewer define el método Entrevistar, que deberá ser implementado por cualquier clase que quiera actuar como entrevistador.
 */
public interface Interviewer {
    /**
    Método que define la acción de entrevistar. Debe ser implementado por cualquier clase que implemente esta interfaz.
    */
    public void Entrevistar();
}
